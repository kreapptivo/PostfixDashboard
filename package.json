{
  "name": "postfix-dashboard-hooks",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "prepare": "simple-git-hooks",
    "pre-commit": "npm --prefix backend run -s format:staged && npm --prefix frontend run -s format:staged"
  },
  "devDependencies": {
    "simple-git-hooks": "^2.13.1"
  },
  "simple-git-hooks": {
    "pre-commit": "npm run -s pre-commit"
  }
}
